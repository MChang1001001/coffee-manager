# 咖啡豆管理项目状态索引

当前阶段：README / 启动文档 / 部署准备

当前状态：MVP 核心功能已完成，全流程 bugfix / MVP 收口检查已通过，当前暂无必须修问题。

## 当前准交付入口

- `README.md`：当前 MVP 启动、配置、联调、验证和限制说明。
- `docs/api-design.md`：当前 MVP 已实现接口概览。
- `backend/src/main/resources/db/init.sql`：数据库初始化脚本。

## 已完成模块

- 后端 coffee 模块。
- 前端 coffee 页面。
- coffee 前后端 MVP 闭环。
- coffee 页面新增 / 编辑弹窗化。
- 后端 file 封面上传模块。
- 前端封面上传联调。
- 后端 review 模块。
- 前端 review 评价功能联调。
- review 模块 MVP 闭环。
- 后端 brew 模块。
- 前端 brew 冲煮记录联调。
- brew 模块 MVP 闭环。
- 全流程 bugfix / MVP 收口检查通过。

## 当前保留决策

- 当前登录继续采用 `admin/admin123456` 临时自动登录。
- 正式登录和 token 生命周期后续统一处理。
- MVP 阶段封面 URL 标准固定为 `/uploads/coffee-covers/{filename}`。
- 不再混用 `/files` 作为图片静态访问前缀。
- 本地 uploads 存储策略 MVP 阶段可接受。
- 云存储、容器持久化、图片删除、旧文件清理延后。
- review 评分步进 0.5 作为 MVP 阶段正式交互规则保留。
- `review_count` / `overall_rating` / `brew_count` 聚合回写延后。
- 风味标签关联延后。
- 枚举查询和字段标准化延后。
- 错误文案中文化延后。
- 正式 UI 组件体系延后。

## 旧草案说明

`docs/咖啡豆管理系统_需求说明_v_1.md`、`docs/咖啡豆管理系统_api草案_v_1.md`、`docs/咖啡豆管理系统_数据库草案_v_1.md` 保留为早期草案参考。当前 MVP 交付状态以 `README.md` 和 `docs/api-design.md` 为准。
