# 咖啡豆管理项目状态索引

当前阶段：v3 阶段总收口 / v0.3 本地版定版

当前状态：v0.3 本地版能力已完成，继续保持本地开发、本地 MySQL、本地可复现启动、手动 SQL 和本地个人使用定位；当前暂无上线部署计划。

## 当前准交付入口

- `README.md`：当前 MVP 启动、配置、联调、验证和限制说明。
- `README-local-exe.md`：Windows 本地 exe 启动器说明。
- `docs/v0.2-release-notes.md`：v0.2 本地版定版记录。
- `docs/v0.3-release-notes.md`：v0.3 本地版定版记录。
- `docs/v0.3-clean-smoke-data.md`：本地 smoke 残留清理脚本说明。
- `docs/api-design.md`：当前 MVP 已实现接口概览。
- `backend/src/main/resources/db/init.sql`：数据库初始化脚本。
- `backend/src/main/resources/db/refresh_coffee_aggregates.sql`：历史聚合字段手动修复 SQL。

## 已完成模块

- 后端 coffee 模块。
- 前端 coffee 页面。
- coffee 前后端 MVP 闭环。
- coffee 页面新增 / 编辑弹窗化。
- 后端 file 封面上传模块。
- 前端封面上传联调。
- 后端 review 模块。
- 前端 review 评价功能联调。
- review 模块 MVP 闭环。
- 后端 brew 模块。
- 前端 brew 冲煮记录联调。
- brew 模块 MVP 闭环。
- v2 Coffee 首页 UI 优化一期。
- v2 养豆期 / 赏味期字段。
- v2 Coffee 档案详情页一期。
- v2 Coffee 详情页最近评价 / 最近冲煮摘要。
- v2 本地 exe 启动器一期。
- 本地烟测脚本。
- v3 聚合回写一期：`review_count` / `overall_rating` / `brew_count`。
- v3 饮用状态筛选一期。
- v3 本地 smoke 残留清理工具一期。
- v3 枚举值 / 常用选项体系一期。
- v3 `variety` 豆种字段。
- v3 中文错误文案与提示体验统一一期。
- 全流程 bugfix / MVP 收口检查通过。

## 当前保留决策

- 当前登录继续采用 `admin/admin123456` 临时自动登录。
- 正式登录和 token 生命周期后续统一处理。
- MVP 阶段封面 URL 标准固定为 `/uploads/coffee-covers/{filename}`。
- 不再混用 `/files` 作为图片静态访问前缀。
- 本地 uploads 存储策略 MVP 阶段可接受。
- 云存储、容器持久化、图片删除、旧文件清理延后。
- review 评分步进 0.5 作为 MVP 阶段正式交互规则保留。
- `review_count` / `overall_rating` / `brew_count` 聚合回写已完成一期；旧库历史数据一致性依赖手动修复 SQL。
- migration 工具暂不引入，继续手动 SQL。
- 饮用状态筛选基于数据库 `CURRENT_DATE`，依赖本地 MySQL / 后端时区一致性。
- 枚举选项一期写在后端静态代码里，枚举接口走现有 JWT 认证体系。
- 赏味期提醒 / 推荐算法延后。
- 风味标签关联延后。
- 统计图表延后。
- 移动端专项适配延后。
- Docker / 上线部署延后。
- OCR 延后。
- 字典表 / 枚举后台管理延后。
- 正式 UI 组件体系延后。
- 完整桌面应用壳延后。

## 旧草案说明

`docs/咖啡豆管理系统_需求说明_v_1.md`、`docs/咖啡豆管理系统_api草案_v_1.md`、`docs/咖啡豆管理系统_数据库草案_v_1.md` 保留为早期草案参考。当前 MVP 交付状态以 `README.md` 和 `docs/api-design.md` 为准。
