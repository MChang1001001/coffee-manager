当前模块：后端 file 封面上传模块
当前状态：准备开始

已完成：
- 后端 coffee 模块
- 前端 coffee 页面基础开发
- coffee 前后端 MVP 闭环
- coffee 页面新增 / 编辑弹窗化
- 新增、编辑、删除、搜索、筛选、分页浏览器烟测通过
- npm run build 通过

当前保留决策：
- 登录继续采用 admin/admin123456 临时自动登录
- 正式登录和 token 生命周期后续统一处理
- roastLevel / processMethod 暂时自由输入
- 正式枚举化延后到枚举查询 / 字段标准化阶段
- 当前弹窗样式为 MVP 原生样式，正式 UI 组件体系后续统一考虑

下一步：
开启后端 POST /api/files/coffee-cover 封面上传模块。